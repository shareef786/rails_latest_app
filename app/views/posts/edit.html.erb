<div class="flex-column">
  <div class="card shadow" style="width: 36rem;">
    <div class="card-header">
      <h2 class="text-center">Update Post</h2>
    </div>
    <div class="card-body">
      <!--Error Message-->
      <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>
      <ul>
        <% @post.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

      <!-- Form Started Here -->
      <%= form_with model: @post, data: { turbo: false } do |p| %>
        <%= p.label :post_title, "Post Title :", class:"mt-3" %>
        <%= p.text_field :title ,class:"mb-2 form-control" %>
        <%= p.label :post_content, "Post Content :", class:"mt-3" %>
        <%= p.text_area :content ,placeholder: "Enter Post Content",class:"mb-2 form-control" %>
        <!-- Checkbox For Hobbies -->
        <%# <div class="form-group">
          <%= p.label "Hobbies :" %>
          <%# <%= p.check_box :hobbies, { multiple: true },"Reading", false %>
          <%# <%= p.label :hobbies, "Reading" %>
          <%# <%= p.check_box :hobbies, { multiple: true },"Photography", false %>
          <%# <%= p.label :hobbies, "Photography" %>
          <%# <%= p.check_box :hobbies, { multiple: true },"Travelling", false %>
          <%# <%= p.label :hobbies, "Travelling" %>
        <%# </div> %>
        <br>
        <%= p.submit "Save Post", class:"btn-primary border-0 rounded-pill shadow p-3" %>
      <% end %>
    </div>
  </div>
</div>